# Evidence reports

One sanitised Markdown report per live check, named `<date>-<slug>.md`, written from the [evidence template](../templates/evidence.md) and linked from the catalogue evidence rows it supports ([verification](../verification.md)). Reports are kept for history even after the requirement text changes; only rows whose digest still matches support a `verified` status.
