"""Lab reuse: capture, restore and rerun against the shared harness.

The Lab owns cases, snapshots and evidence. Task evaluation calls the
embedded Deep Agents harness (MOD-005). Engine measurement is llama-bench.
This is not a second evaluation agent loop and not Chat or Builder.
"""
