{
  "$comment": "GENERATED by workbench_backend.contracts \u2014 do not hand-edit. Regenerate with the registered shared-contract command.",
  "$defs": {
    "RunLifecycleStatus": {
      "description": "Shared run lifecycle states, including honest cancel request vs confirmed stop.",
      "enum": [
        "queued",
        "running",
        "cancel_requested",
        "cancelled",
        "completed",
        "failed"
      ],
      "title": "RunLifecycleStatus",
      "type": "string"
    },
    "RunStreamEventType": {
      "enum": [
        "snapshot",
        "run_event",
        "stream_end"
      ],
      "title": "RunStreamEventType",
      "type": "string"
    },
    "SharedAgentEvent": {
      "description": "One application harness event. Not a raw LangGraph stream chunk.",
      "properties": {
        "at": {
          "title": "At",
          "type": "string"
        },
        "detail": {
          "additionalProperties": true,
          "title": "Detail",
          "type": "object"
        },
        "kind": {
          "title": "Kind",
          "type": "string"
        }
      },
      "required": [
        "at",
        "kind"
      ],
      "title": "SharedAgentEvent",
      "type": "object"
    }
  },
  "description": "JSON `data` for each SSE message on GET /v1/events.",
  "properties": {
    "conversation_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Conversation Id"
    },
    "event": {
      "anyOf": [
        {
          "$ref": "#/$defs/SharedAgentEvent"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "run_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Run Id"
    },
    "seq": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "1-based AgentEvent index on the run. Set on run_event; used as SSE id.",
      "title": "Seq"
    },
    "snapshot": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "GET-equivalent agent-run or chat-conversation record.",
      "title": "Snapshot"
    },
    "status": {
      "anyOf": [
        {
          "$ref": "#/$defs/RunLifecycleStatus"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "type": {
      "$ref": "#/$defs/RunStreamEventType"
    }
  },
  "required": [
    "type"
  ],
  "title": "RunStreamEnvelope",
  "type": "object"
}
