# Shared-contract generated artifacts

JSON Schema, OpenAPI, and the generator manifest in this directory are **generated**. Do not hand-edit them.

Source of truth: `apps/backend/src/workbench_backend/contracts`.

Regenerate and check freshness with the commands in `specs/commands.md`.
