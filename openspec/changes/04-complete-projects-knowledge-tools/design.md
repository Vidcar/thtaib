# Design: Complete projects, knowledge and external tools

## Technical Approach

Extend the existing effective-setup resolver, harness/backend, knowledge/retrieval services, tool catalogue and Project/Knowledge/Chat configuration panels. Reusable agent setups reference existing profiles, tools and knowledge; they do not become another model or settings store. Compose ordinary instruction layers in the agreed order while enforcing access separately.

Keep project storage, retained originals, extracted source views and framework scratch distinct. Use official `memory=` and `skills=` integration for selected content, with protected instructions in composed system instructions. Refresh supported middleware state and only obsolete derived resources at turn boundaries; never reset the thread or delete framework history. Full skill packages include safe relative resources, not only SKILL.md. Moving an authorized resource to an execution environment is separate from loading it in a virtual backend.

Choose compatible local parsers for text/code, CSV, JSON, text-bearing PDF and DOCX. Preserve actual source ranges and parser versions; do not add a hosted extraction dependency. Existing derived retrieval remains opt-in and must validate real embeddings. Ordinary document reading does not depend on indexing.

After `migrate-local-agent-interaction` is implemented and verified, reuse its shared `@langchain/react` projections/subscriptions for common message, tool and run presentation. Keep configuration, connection and selection controls in the existing application-owned shared views, resolving the same versioned setup and authorization records. Use the repository's supported official LangChain MCP adapter and one shared async harness driver. This change owns migration of invocation/streaming, all middleware hooks, saver/state access, resume, cancellation and shutdown as one compatible path. The completed prerequisite proved public synchronous `stream_events` and retained that driver. Implement the complete async transition here, preserving the verified interaction boundary rather than splitting or duplicating drivers. Reuse the existing checkpointer database and public APIs; keep long-lived loop-bound resources on their owning loop. Ordinary Chat and Lab must still work without MCP.

Implement one concrete public web search plus page-reading integration; choose and document the provider/authentication/limits without assuming a paid account or substituting a documentation MCP. Preserve external content as untrusted task data. No second permission gate is required merely to use supplied content through tools already authorized for that task.

## UX presentation

Follow the shared UX contract in `../03-complete-shared-chat/design.md#shared-ux-contract`. This change owns the everyday workspace presentation for reusable agent setups, project controls, scoped Knowledge, file previews/diffs, proposal review and connection management. The primary journey must be understandable without raw JSON, internal identifiers or backend terminology; technical evidence, provenance, parser details, dependency records and backend diagnostics remain available in expandable details.

Expose reusable setups through the **Agents** destination while keeping the active setup selectable from the conversation controls defined by change 03. Project identity stays in the fixed conversation area and header; changing project area starts or opens a different conversation rather than detaching an existing one. File previews, diffs, changed-file review, document source inspection, memory proposals and tool activity use the shared right-hand panel and Library/retention records instead of separate stores. Connection add/edit/test/disconnect, saved permissions, appearance, notifications and manual backup/restore belong in **Settings**.

Technical verification and Dave's UX acceptance are separate completion records. Everyday workspace UX acceptance is currently pending, not accepted. It covers a built Windows journey at full-window and half-screen sizes, Windows display scaling, keyboard navigation, long content, and at least one failure/recovery state across a project file task, document-backed answer, memory proposal, skill resource read, connection lifecycle and ordinary Chat after the async transition. UX acceptance remains pending until Dave accepts the built journey or explicitly defers it.

## Failure and migration handling

Preserve real user scope/version identities and compatible existing threads. Expose unsupported checkpoint/session/client behaviour rather than deleting state or inventing compatibility. Record remote tool errors separately from transport/conversion failures. Imported packages, document text and discovered tools cannot authorize themselves. Extend the shared deletion/manual-backup contract for every new record family.

## Integration references

Use the checkout's locked versions; these are integration entry points, not permission to upgrade the stack.

- [Deep Agents integration guide](https://docs.langchain.com/oss/python/deepagents/overview)
## Authorized delivery refinement — 2026-09-22

Dave confirmed Packet 03's Explorer drop, notifications and tray checks and requested the following adjoining repairs while completing this packet. Reuse existing owners and stores: a compact searchable Library with selection and on-demand previews; Knowledge deletion, clear scope and actual project/setup relationships; unified model discovery, reliable visible downloads, durable capability status, explicit vision companion association and permanent model deletion; clear compact notification settings. Retained image uploads and clickable image/screenshot previews are advanced from Packet 08, using actual multimodal request bytes and the existing scoped retained-file owner. Model screenshots/tool image blocks must be viewable without opening technical activity details. Validate actual Gemma and Qwen image behavior; a companion file on disk is not proof that it is loaded or works. Do not infer final UX acceptance from implementation or tests.


## Implemented boundaries and technical evidence — 2026-09-22

Projects use canonical folder IDs and inherited defaults. Agent setup edits create immutable versions; the Chat selector applies a chosen version only to future work. Sparse overrides preserve inherited values and explicit empty selections. Queued turns freeze selected versions and backend-composed instruction layers. Existing threads, branches and historical configuration remain readable after current dependencies change or become unavailable.

Knowledge uses real project/agent scope options and backend-owned actor/run provenance. The same owner stores memory, protected instructions, versioned skill manifests/resources, proposals and exact-destination automatic-save permission. Same-thread checks exercise actual memory prompt refresh and skill reads across v1 → v2 → deselection while preserving history/offloads. Projects, setups, Knowledge/packages, connections and credentials expose shared dependency previews naming future-use and historical consumers before removal, disable or disconnect. Source folders, immutable versions and historical evidence remain; disconnect does not claim remote cancellation or provider revocation.

Verified project edit/create/delete/rename records capture actual pre/post hashes and bounded text. The right panel displays a supported diff or an honest unavailable reason, and reversal verifies current content before writing. Original documents/images remain in shared retention, with separate local extraction, actual source version/range navigation, read authorization and derived retrieval. Package loading never executes imported scripts. Backup uses the existing application/checkpoint/knowledge/asset roots; connection references restore without credential values, and missing external dependencies remain explicit.

The common driver now owns AsyncSqliteSaver, native async streaming, model/tool sessions, interruptions and cleanup on one loop. Six lifecycle regressions cover synchronous-checkpoint continuation, async-only session lifetime across a user-input wait, cancellation during model work, settlement of executor-backed local effects before releasing project ownership, nonblocking telemetry and failed saver setup cleanup. Eight file-change regressions and focused scope/setup/package tests passed. Twelve connection regressions exercise the official protocol/adapter, namespace/schema/result identity, tools-off, errors, unsupported elicitation decline, session disconnect/cancellation, unknown external outcomes and parallel-call serialization. Three API/store dependency-preview regressions verify actual future/default/Chat consumers, retained package resources and no secret/content disclosure.

Public web uses the explicitly selected Brave public search backend through the locked DDGS integration; it requires no paid account or API credential. Queries are sent to that provider, limited to eight results with a 15-second timeout; unavailable/rate-limited search reports failure without provider fallback. The separate public HTML/plain-text reader enforces public-network addresses, a 25-second timeout, a 2 MB response bound and explicitly marked 40,000-character output truncation. Search snippets and fetched passages carry source/time and stay untrusted task data. MCP HTTP/stdio connections use the supported adapter and tested tool schema; HTTP tokens remain backend-only in the Windows credential vault.

Live Gemma completed documentation MCP search → framework-scoped offload read → sourced answer (`agent_d8ee8dfd10c6`) and, separately, public web search → actual documentation page read → sourced answer (`agent_e8768d9d6d63`). Developer scratch results are `.scratch/packet04-live-mcp-result.json` and `.scratch/packet04-live-web-result.json`; these are live-model evidence, not substitutes for the UI journey. Native Windows checks passed project folder picker/create, retained image upload/thumbnail/viewer/actual Gemma image response, permanent model-copy deletion with disk/metadata verification, and the real Windows credential vault. Qwen's explicitly selected companion passed the paired-image probe (`probe_77a8af1e0f66`) and a fresh tool-call receipt probe (`probe_02d2e062c1a0`), with current saved evidence reread.

The full desktop typecheck/build and component/Electron checks passed after source-reference, selected-model and lifecycle-preview integration. Components verify conflict-preserved drafts, exact scope/provenance, future-turn/queue setup isolation, late-response selection guards, write-only credentials, removal preview failure/duplicate submission and reversal conflicts. Rendered fixture geometry passed 36 cases across six workspace surfaces, dark/light themes and actual widths 601/795/1281 DIP; fixture screenshots do not establish successful native interaction or user acceptance. A subsequent same-thread original → updated → deselected retrieval regression verifies actual search sources and new offload bytes with explicitly fake embeddings; all eighteen retrieval/project tests pass. Remaining checks are the complete Windows journeys in tasks 2.8/2.9, while final sequential backend verification has passed 612 default tests and 156 integration tests, with no failures or reported skips. Dave's final UX acceptance remains pending.


Follow-up native findings: at a 720-DIP window with expanded navigation, the 446-DIP content area kept an unusable two-column Knowledge editor because the global surface rule replaced its container name. The scoped container declaration now preserves both names; Projects/Agents/Knowledge stack list/detail based on available width and preserve two columns at wide widths. Library preview breakpoints also use available content width. An expanded-navigation Electron geometry fixture now asserts stacking at 515/437/363-DIP surfaces and two columns at 951 DIP in both themes, including the expanded connection-removal preview (56 cases), correcting the earlier insufficient overflow-only fixture. Native Knowledge create, real project scope, save, dependency preview and remove passed.

Actual React/Electron source-viewer checks exercise exact source range/scope requests, long text, light/dark themes at 601/795/1281 DIP, failed reads and native modal focus. They reproduced focus returning to the page body when closing either a source or image viewer; both now close the native dialog before unmounting and restore the originating citation/thumbnail. The permanent viewer regression is included in the desktop build. Fixture geometry and focus checks remain separate from Dave's acceptance.


Final automated verification: 612/612 backend default and 156/156 integration tests passed sequentially against the combined implementation, with no reported skips. The complete desktop build includes Electron trust, source/image modal focus, actual-component controls and navigation checks; the connection-preview adjustment passed expanded-sidebar geometry and the subsequent full desktop build. Strict OpenSpec validation passes all 16 items. This evidence does not complete the remaining native journey or user-acceptance tasks.

Final native review found a project attachment/send 409 caused by an absent workspace being sent as explicit null beside the canonical project ID. The frontend now omits that conflicting assertion; real-component picker and drop regressions verify project-bound create/upload/send/draft scope and preserve the task and file on rejection. A separate startup history failure had hidden every navigation control; independently successful refresh data now remains usable with Retry inside the shell, and a confirmed deleted-chat selection returns to New Chat. Both regressions were reproduced before repair. The updated Windows project CSV/document/file/proposal/skill journey remains pending after these fixes; tasks 2.8/2.9 and final UX acceptance stay open. Native half-width Knowledge, HF repository selection, saved Qwen capability evidence and exact model deletion passed.
