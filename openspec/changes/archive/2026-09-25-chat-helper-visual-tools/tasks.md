# Tasks

## 1. Helper ownership and visibility

- [x] 1.1 Record helper activity before model admission and settle every outcome; verify focused backend tests for waiting, failure, cancellation and interruption.
- [x] 1.2 Stream one child execution under its unique saved scope and replay public child events; verify single, parallel and reopened helper tests.
- [x] 1.3 Render the exact parent delegation request and status in Chat, expose live and saved child transcripts in the rail, and remove the duplicate Helpers button; verify desktop interaction checks and keyboard access.

## 2. Browser and Windows capability controls

- [x] 2.1 Fix request capture for nested browser tool schemas while retaining media redaction; verify a recording-transport regression.
- [x] 2.2 Preflight Browser worker and lost session at readiness and Send with corrective actions; verify ready, missing and lost backend cases.
- [x] 2.3 Put all Browser and Windows selection, install, grant and recovery controls in the composer menu and remove the Setup duplicate; verify desktop flows, pre-model access and narrow layout.
- [x] 2.4 Make next-turn tool selection truthful and remove incidental capture reading on capability disable; verify Browser and Windows On-to-Off and independent project/knowledge selection cases.

## 3. Delivery

- [x] 3.1 Run backend default and integration suites, desktop build, generated-contract freshness when wire types change, OpenSpec validation and diff check; repair relevant failures.
- [x] 3.2 Run isolated native Chat checks for live helper delegation/reopen and Browser use, preserving everyday product data; record the observed result in the existing handover.
- [x] 3.3 Sync the implemented contract into current OpenSpec specs and verify the final contract and Git diff before archiving.
