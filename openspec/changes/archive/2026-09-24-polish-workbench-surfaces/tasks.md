# Tasks

## 1. Configuration and runtime

- [x] 1.1 Reproduce known default, inheritance, duplicate configuration and port-conflict defects with production boundaries; retain the failing cases as regression evidence.
- [x] 1.2 Implement versioned model defaults/variants using existing storage and an idempotent quiet migration; verify exact effective settings, instructions, historical snapshots, weights and ScratchArea records survive.
- [x] 1.3 Extend the shared setup resolver with source/default/support/reset/reload facts; verify all controls display the same values the run and captured request use, including explicit empty/default/reset and stale responses.
- [x] 1.4 Add coordinated reconfiguration and launch-time port allocation/preflight; verify idle continued Chat, expected revisions, history fit, all durable consumers, ownership, failure restoration and restart reconciliation.
- [x] 1.5 Replace primary preset/deployment controls with canonical model configurations in Models, Chat, Agents, project/application defaults, Lab and Workflows; verify Save changes, Save as variant, local Apply and Save to model are distinct and non-duplicating.

## 2. Access and agent capabilities

- [x] 2.1 Implement Ask/Approve for me/Full access semantics and truthful inherited access; prove receiver-side mutations, recovery coverage, saved-grant attribution and disabled-tool enforcement.
- [x] 2.2 Implement frozen Work/Plan mode and backend read-only enforcement across ordinary/restored/child calls; prove prohibited actions have zero effects even under Full access, while authorized reads/questions/planning work.
- [x] 2.3 Implement selected frozen named helper versions through shared enforcement and upstream delegation; verify authority intersection, no recursive/general helper, named streaming/approvals, shared cancellation and model resource admission.
- [x] 2.4 Implement opt-in read-only rubric review with visible criteria and at most two revisions; verify no reviewer call when off, no reviewer side effects, genuine distinct judgement and visible limit reached.
- [x] 2.5 Enforce explicit atomic shared tool-call budgets and remove accepted-but-unused review semantics; verify concurrent child calls cannot overspend and unset limits remain unset.

## 3. Stable conversation and shared controls

- [x] 3.1 Reproduce and remove composer/menu/telemetry jumps using stable overlays, reserved measurement width and compact attachments; verify rendered geometry, scroll position, Escape/focus and long labels.
- [x] 3.2 Restore conversation, draft, attachments and reading position across destinations; verify explicit New chat alone resets state and sidebar/header titles agree.
- [x] 3.3 Implement truthful thinking toggle/effort/context controls and local apply/reload; verify actual supported defaults, unavailable reasons and dispatched settings.
- [x] 3.4 Expose Work/Plan, selected helpers and optional review without a wall of switches; verify next-turn scope and immutable submitted/queued settings.
- [x] 3.5 Group adjacent finished successful activity while preserving every identity; verify live/failed/waiting separation, result-first disclosure, checklist marks, incoming turn and saved permissions.
- [x] 3.6 Group a file's changes without merging recorded differences and make editor failures actionable; verify actual packaged editor/worker loading and selecting/reversing the exact edit.

## 4. Existing surfaces

- [x] 4.1 Move project settings/files/lifecycle into rail context and remove redundant destination; verify removal versus deletion, file ownership and context navigation.
- [x] 4.2 Use compact Models/Agents list-detail editors with useful descriptions, sources and actionable disabled controls; verify create/edit/save/apply/reopen and failure paths.
- [x] 4.3 Make Knowledge, Library/files and Attention dense and purposeful; verify versions/proposals/skills, source previews/reuse/deletion and correctly owned attention actions.
- [x] 4.4 Organize Settings by direct categories, compact basics and deliberate advanced appearance/preview; verify saved customization, Apply/Cancel/Reset and preview with guides initially off.
- [x] 4.5 Repair current Lab and Workflows actions using shared settings/access/results; verify each existing visible action and failure/retry without claiming the deferred suite/canvas complete.
- [x] 4.6 Verify responsive bounded layouts, both themes, narrow/full/ultrawide windows, Windows scaling, keyboard access and no horizontal overflow or layout shift across all surfaces.

## 5. Delivery gates

- [x] 5.1 Run backend default and integration suites, desktop build, generated-contract freshness and strict OpenSpec validation; retain meaningful permission/process/recovery regressions and replace obsolete markup assertions.
- [x] 5.2 Complete native file picker/Explorer drop, retained document/diff, real-model reasoning/tool/context, Plan/helper/review/cancellation and recovery journeys in isolated roots; record actual outcomes separately from mocks/builds.
- [x] 5.3 Review every visible function against its real effects, repair discovered defects and reconcile all change artifacts; no checked task may represent partial/deferred work.
- [x] 5.4 Deliver the validated branch/PR and established local desktop/backend, obtain remaining visual acceptance, then merge/archive only when gates pass; refresh HANDOVER.md with verified state and any concrete next step.

## Implementation and acceptance evidence

- Reproductions: null inherited access displayed Ask while dispatch inherited Full; literal Qwen template default xhigh hidden; duplicate saved runtime records shared port8080; open composer details changed trigger geometry; leaving Chat cleared selection. Backend/component regressions now exercise these boundaries rather than obsolete markup.
- Final backend default689 and integration168 pass on6885a4d, including canonical origin/migration, interrupted-runtime recovery, immutable Lab/helper requirements, receiver-side access, exact saved-permission attribution and cooperative shutdown boundaries. Generated contracts match. Strict OpenSpec validation passes. Final full desktop build on3ae0da9 passed including rendered transcript follow, settings ownership/reset, Knowledge/Workflows action journeys, shared titles, loaded-model saving, startup conversation restoration, saved-permission details and packaged Monaco verification.
- Real CPU Gemma: launch-time automatic port avoided occupied8080; owned4096 context became8192 in an idle continuing chat; next turn recalled earlier history; shrink2048, paused queue and waiting Lab refused without stopping the model. Failed real launch restored healthy8192 once. Evidence `.scratch/unified-runtime-live-20260923/evidence.json`.
- Real Qwen connected: Plan produced no prohibited effect; Ask rejection wrote nothing; Approve retained the project preimage; Full wrote once; selected Reader produced a durable child record; bounded review produced an independently captured review request and judgement. Evidence `.scratch/unified-workbench-uat/agent-smoke-results.jsonl`.
- Native Windows: file picker retained sample.txt; draft/attachment survived Settings navigation and desktop restart/reopening. Light/dark Settings and keyboard Escape/theme selection passed at actual Windows200% scaling. Attached text reached the real model (PINE-42). Retained Word preview showed CEDAR-58. Ask held a real write until one-time approval; packaged Monaco showed its exact diff and reversing it removed that file. Hidden Monaco paint leakage was reproduced and fixed without losing retained reading position.
- Final20 overlay/viewports at720/1280/1920/3440 measured zero composer movement/reading displacement and no horizontal overflow. Final341 real-stream samples show unchanged860px composer/120px telemetry and completed-answer bottom gap0.5px. Production rendered regression reproduces/fixes late answer actions and approval arrival, with PageUp/Latest user intent. Native approval arrived fully in view with Waiting/12characters proposed; rejecting it produced no file and settled the turn. Evidence `.scratch/unified-workbench-uat/live-geometry.json` and `geometry.json`.
- Native Models: clearing saved temperature previews unknown model default, Save changes preserves its canonical id with revision3 and no new deployment, and identical Save as variant remains a distinct named configuration. Evidence `.scratch/unified-workbench-uat/native-configuration-save.json`. Native project rail settings/files and new-project-chat action passed. Appearance13→14 preview/Cancel, Apply, individual Reset→13/Apply, and independent preview with spacing guides off passed.
- Real surface APIs verify original/project files survive link, Chat and retained-asset deletion, and Qwen Lab capture/restore/recorded+live replay/approval/cancellation preserve frozen configuration. Evidence `.scratch/unified-workbench-uat/surface-api-results.jsonl`.
- Production-component receiver journeys cover Agents create/edit/reopen/use exact version/history/duplicate/remove and Knowledge version/save/restore/rename/enable/remove/proposal rejection/package import+update/capture. Failed saves/imports preserve drafts and retry. Workflows canonical configuration Apply/submission, exact approval response and failed registration/reopen retry are covered; failed opening no longer dismisses Attention or resubmits work. These are component/service results, separate from native Windows journeys.
- Responsive production-App matrix passed120/120 cases:15 views including contextual Projects and all Settings categories, four widths720/1280/1920/3440, both themes. No horizontal overflow, fields≤742px, visible Tab focus, narrow pane collapse and no renderer errors. Previously932–1166px Models/Knowledge/Workflows fields are capped. Evidence `.scratch/responsive-qa/results.json`; this is rendered Electron evidence, supplemented by native Windows200% checks.
- Everyday backend/renderer updated with Qwen's original process/context preserved, all15 weight paths unchanged, all application/checkpoint table hashes unchanged (9chats/19runs). Native reopening retained ScratchArea history, draft and Full access; actual thinking displayed On/Xhigh model defaults. Reproduced legacy summary/full title mismatch and missing loaded-runtime Save target are fixed: native sidebar/header now agree, Use loaded model names the real source and enabled Save identifies Qwen's Default configuration. Saving uses the loaded startup/response recipe without implicitly pinning/applying the target; a deleted historical alias falls back safely.
- Final native renderer restart automatically restored the selected ScratchArea chat, draft, history, reading position and Full access. Backend-scoped UI preference preserves explicit New; failed catalogue loading retries, missing records clear only after a valid read, and a delayed restoration cannot override manual selection or submit into a blank conversation. Production App/Chat receiver checks cover these races in the normal desktop build. Inherited project/agent configurations also name their model before it loads, and opening never falsely says Sending.
- Reconciled21 stale existing-header ADDED delta blocks in overlapping changes and preserved every current scenario. Synced the four affected main capabilities and reconciled overlapping access, reload and activity contracts so later archives cannot roll them back. Unrelated acceptance gates remain separate.
- Isolated cleanup reproduced an open observer keeping Uvicorn waiting after durable work stopped. Shutdown2134079 now signals observer teardown before Uvicorn drains, with a bounded fallback. Ten focused lifecycle/event-stream checks and both final suites pass, including a held real HTTP observer reaching clean EOF/process exit without forced cancellation and failed Quit retaining another maintenance owner's gate. Observer teardown remains separate from execution authority. Isolated validation processes are stopped.
- On2026-09-24 Dave completed the native Explorer drag and confirmed the interface looks good. Root independently observed the image attachment chip and thumbnail in the composer with the original draft retained. This closes the formerly blocked cross-window drag and visual-acceptance gates; the computer-use helper itself cannot perform that cross-window gesture. Final backend refresh compared every SQLite table against a fresh pre-refresh snapshot, preserving the new attachment,9 chats,19 runs and all15 weight paths. Native post-refresh confirmation retains the image/draft and healthy loaded model. The attachment remains Dave's unsent draft.
- Final spec-sync audit preserved existing grant/lifecycle requirements and identified missing API037 grant identity. Reproduced actual granted rename and rendered-activity failures are fixed by6885a4d/3ae0da9: exact matched grant snapshot is captured at authorization and retained in ToolMessage, durable result, helper and restored-run projections. Activity names that grant and further disclosure shows its recorded scope/arguments; historical source-only records stay generic. Incoming tool metadata cannot forge attribution. Focused receiver/component tests and the final desktop build pass; permission matching remains unchanged.
