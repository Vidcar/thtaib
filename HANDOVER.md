# Current handover

Updated 2026-09-25. Chat model selection/readiness repair is complete. The nullable readiness 500, lost model choice after agent selection, stale turn warning, blocked Queue, redundant picker checks, repeated full hash of resident weights, and stale sidebar title are repaired. Current Chat/Models OpenSpec requirements include the behavior; the completed change is archived at `openspec/changes/archive/2026-09-25-chat-selection-readiness/`. The older `startup-catalogue` delta retains its own intent and the new New Chat scenarios.

Checks: backend default 827 passed (one skipped), integration 192 passed; desktop build, shared contract check, OpenSpec validation, and diff check passed. Native checks using the existing saved `deploy_dcda6b62b64d` found New Chat kept the model, exact healthy reselection made zero readiness/load requests, saved content appeared in 33 ms, and initial Send/reply admission took 728/821 ms (about 10.9 seconds before the backend repair). The sidebar title updated after the completed turn. Disposable test chats were deleted; existing chats, drafts, settings, and model files were retained. The local app is to run from merged main with the same saved deployment.

Separate main-checkout work remains untouched: `openspec/changes/lab-workbench/` is validated Lab planning, not implemented. It covers the Lab destination, Performance (Single stream and Concurrent serving), Memory, and Challenges. Llama-bench and exclusive hold were removed; case capture remains for the older screen. Workflows, image, and speech are deferred. Its build slices are in that change's tasks.
