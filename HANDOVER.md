# Current handover

Updated 2026-09-23. Chat streaming, recovery, Access and presentation repair complete and running locally; delivery branch `codex/chat-stream-access-repair`. Preserve model weights and ScratchArea chats.

Reproduced and fixed snapshot/status/cursor inconsistencies, compaction during hydration/reconnect, finished-message loss, incomplete SSE cursor advancement, cancelled paint frames, incorrect fixed-height scrolling, Markdown fragmentation, stale tool results, competing scroll owners, and startup queue/deletion races. Access follows the selected conversation and current inherited setup; serialized null and changing saved agents cannot silently retain Full access. Queued settings stay frozen. Full labels explain next-message scope. Partial tool input stays visibly unfinished; completed file bodies retain readable lines. The narrow rail leaves room for the composer.

Verified: backend default 640 and integration 158 pass; shared contracts current; strict OpenSpec 14 and desktop build pass, plus final renderer hydration regression. Native isolated Qwen checks: Ask paused and rejection wrote nothing; Full executed once; long reasoning and a 160-line file streamed, reader scroll stayed put, switching conversations resumed the same run, and final table/code rendered. First/last file lines and half-screen rail checked. A fresh cancelled write retained all 512 argument characters after reopening with zero execution/file effects. Evidence: `.scratch/chat-access-live-20260923/`. `chat-stream-follow` tasks complete and specs synced, awaiting archive; Dave's UX acceptance remains separate.

Everyday backend8000/desktop updated with the final fix. A stale post-approval run recovered as failed/orphaned after cancellation stuck; all 34 messages, 57 checkpoints and recorded file/tool effects preserved, no replay. Model8080 remained running. Its original blocked worker's exact cause was not established; do not equate recovery with a reproduced fix for that wait.

Other work preserved: untracked `polish-workbench-surfaces` proposal; implemented `startup-catalogue`/`rewrite-loaded-boundaries` awaiting archive; unfinished `consolidate-product-contract`; prior Packet04 journeys and Dave's visual acceptance remain unaccepted. No new trackers or CI.
