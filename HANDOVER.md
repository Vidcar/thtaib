# Current handover

Updated 2026-09-24. The pinned Hugging Face model-card recipe and truthful model-control change is implemented on `codex/model-card-recipes`. Its contract is synced into `openspec/specs/models/spec.md`; the completed change is archived at `openspec/changes/archive/2026-09-24-model-card-response-recipes/`. Git delivery remains. Preserve the unrelated active `consolidate-product-contract` change.

The downloader honors an exact `show_file_info` GGUF hint, distinguishes Standard/LOW-MTP/MTP files, imports conservative revision-pinned card recipes separately from automatic publisher defaults, accepts valid zero samplers and penalties, and creates idempotent named model configurations. Existing bundles can refresh card metadata without weights. The desktop shows backend-resolved value/source and retains inheritance on edits. The ordinary backend is healthy on port 8000; the updated development desktop is running. No active import or model probe remains.

The exact LOW-MTP IQ4_XS is installed as `bundle_82b658fd3dd6` beside the retained standard bundle `bundle_c8ee61eba37d`. Its weight hash matches the pinned Hugging Face file. General thinking `profile_572cdbf5998f` is the LOW default; Precise coding `profile_c0ced5a578a0` and Non-thinking `profile_c2b4c5026cbc` are also saved. A real isolated llama.cpp b11045 load of the LOW file verified its thinking template and all six transmitted sampling values per recipe; Non-thinking sent `enable_thinking=false` and returned no reasoning content in the probe. The standard bundle's metadata-only refresh preserved its weight and setup records. Vision inference was not exercised.

Checks: backend default 751 passed, one skip; integration 188 passed; desktop `pnpm run build` passed; generated contracts fresh; OpenSpec validation 14/14; narrow browser pointer/keyboard fixture passed at 345 px (mock API). Next: inspect Git diff, commit/push/merge, then confirm the local app remains healthy. Shell commands must use RTK.
