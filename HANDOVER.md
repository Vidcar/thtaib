# Current handover

Updated 2026-09-25. Visual agent setup regression fixed: a pre-PR #157 backend was still running on port 8000, advertising 13 tools and rejecting the new `desktop_access` field with 422. The editor now names missing tools and prevents incompatible saves; launcher and Electron detect a stale service and give a visible restart path. The service was stopped through its supported endpoint and relaunched; the same managed deployment (`deploy_9ce15eac34c2`) is healthy with its profile, bundle, and startup/settings values unchanged. The live backend advertises 41 tools; a 41-tool Selected-window agent setup saved and its validation record was removed from the active list. Pinned Playwright MCP and WinApp CLI workers are installed in product data; a local browser fixture passed navigation, snapshot, click and screenshot, and WinApp enumerated windows and inspected/captured the Workbench window. Backend default: 788 run, 1 skip; integration: 191 passed; desktop build, contract freshness, OpenSpec validation, launcher check and diff check pass. Six tools in projectless Chat with no optional tools selected remains expected. Visual testing's archived contract is at `openspec/changes/archive/2026-09-25-agent-visual-testing/`. Preserve unrelated work, product data and weights.

Prior GPU layer control work on `codex/fix-gpu-offload-controls` labeled `-1` Auto and literal `all` All. Dave asked for the control fix only; **do not change his saved model configuration or running deployment**. The previous 98k live configuration was restored after testing. See Git history for delivery state.

Previous card-import work: PR #155 is merged; see Git history.
