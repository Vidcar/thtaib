# Current handover

Updated 2026-09-25. Visual testing is delivered through PR #157 (`codex/agent-visual-testing`): bounded Deep Agents 0.7.19 image reads, exact user/tool vision probes, retained `/captures/` assets, optional pinned Playwright MCP and WinApp CLI workers, owned preview, scoped Windows grants, and compact Chat controls. Isolated real vision project Chat identified a known image; a real browser Chat run navigated, clicked, inspected console, captured, then read its image with the correct tool ID and final URL. Two disposable Windows windows verified Selected/All scope, accessibility and capture. Browser Reset/Close worked across Chat turns; preview Windows checks covered launcher exit, idle expiry and cancelled launch. Hidden Electron pointer/keyboard checks passed at 190px and 380px. Backend default: 787 pass, 1 skip; integration: 191 pass; desktop build, generated-contract freshness, OpenSpec validation, diff checks, and backend wheel manifest check pass. Main specs are synced and the change is archived at `openspec/changes/archive/2026-09-25-agent-visual-testing/`. No implementation follow-up remains; verify PR #157 is merged and the local checkout is clean. Preserve unrelated `consolidate-product-contract`, product data, weights and the running deployment.

Prior GPU layer control work on `codex/fix-gpu-offload-controls` labeled `-1` Auto and literal `all` All. Dave asked for the control fix only; **do not change his saved model configuration or running deployment**. The previous 98k live configuration was restored after testing. See Git history for delivery state.

Previous card-import work: PR #155 is merged; see Git history.
