# Current handover

Updated 2026-09-25. Dave's previously installed models have been checked against the updated Hugging Face card flow. PR #155 is merged on `main`: the conservative card parser now recognizes Unsloth's explicitly recommended, numbered Best Practices sampling section. PR #154's desktop UI unification remains in place. The ordinary backend was restarted with the fix and the Local AI Workbench desktop remains open.

The revision-pinned DavidAU standard bundle `bundle_c8ee61eba37d` and LOW-MTP bundle `bundle_82b658fd3dd6` were refreshed in place from their verified saved cards. Each offers General thinking, Precise coding, and Non-thinking; existing configurations and files were unchanged. The older local Unsloth bundle `bundle_e1dee66362b1` has no pinned HF source. Its weight and projector hashes exactly match `unsloth/Qwen3.8-27B-GGUF` at revision `4ca720788d1e01f1bff70c033e0d0028fd02e502`. A separate HF-managed bundle `bundle_22c1dc9ef173` was imported with those files, the card, and configuration metadata. Its card offers Thinking and Non-thinking; both named configurations were created, while its default stayed on Default. The original local bundle, setups, and deployments remain intact. The new copy uses about 15.2 GB of additional model storage; no live inference was run.

Checks: 752 default backend tests passed, one skipped; 188 integration tests passed; live pinned-card inspection, import completion, model-file identities, card, and configurations verified. The backend health and desktop window were verified after restart. No further card-import work is pending. Preserve the unrelated active `consolidate-product-contract` change and model weights. Shell commands use RTK.
