# Current handover

Updated 2026-09-24. Models workspace redesign is implemented on `codex/models-workspace-redesign` in [PR #139](https://github.com/Vidcar/thtaib/pull/139). The completed OpenSpec change is under `openspec/changes/archive/2026-09-24-models-workspace-redesign/`, with MOD-025–027 synced to `openspec/specs/models/spec.md`. Models now has My models, Add models and Downloads tabs; a grouped GGUF variant table; directly visible saved model controls and provenance; and terminal download record clearing. Repository inspection separates MTP and imatrix GGUFs from primary weights. The shared API contract is regenerated.

The desktop build and its model/UI checks passed. Backend default passed 706/706 and integration passed 168/168. Shared-contract freshness and strict OpenSpec validation passed. An isolated Windows preview of the built renderer and backend downloaded a real Qwen2.5 0.5B Q4_0 GGUF, saved sampling and 4k context settings, loaded it, then safely reloaded at 8k as reported by the engine. Tab persistence, background completion, keyboard selection, narrow layouts, long failed names and discard were checked. The isolated preview services are stopped; its disposable data remains under `.scratch/`.

The everyday backend and desktop were restarted from the updated checkout. Its existing 27B managed deployment is healthy again. Exactly three old discarded job records were cleared through the fixed endpoint; shared completed-install staging was compared before and after and preserved. Eight completed job records remain. Model weights and other product data were retained.

Next: push the OpenSpec sync/archive and handover update to PR #139, mark it ready and merge, then verify the final merged desktop. Prior delivered work includes [PR #138](https://github.com/Vidcar/thtaib/pull/138) and [PR #137](https://github.com/Vidcar/thtaib/pull/137).
