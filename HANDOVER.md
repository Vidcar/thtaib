# Current handover

Updated 2026-09-25. GitHub `main` includes PR #154, the cloud agent's desktop UI unification. The prepared Windows checkout has that version; its production desktop was rebuilt and relaunched. The Local AI Workbench window is open, and the existing backend on port 8000 is healthy. The change updates controls in Appearance, Defaults, Connections, Models, and the chat model menu. Its contract is `API-042` in `openspec/specs/backend-desktop/spec.md`.

Windows `pnpm run build` passed, including typechecks, chat interaction boundaries, Electron trust boundary, retained viewers, and all other desktop checks. The cloud VM's three reported checks were resolved by this local run; no code change was needed. Build output still includes non-failing React test renderer and bundle-size warnings. The cloud agent's screenshots cover light/dark, compact/comfortable, and 1280/360 px; a fresh native visual review on this Windows launch has not been done. Surfaces restyled with tokens but not restructured include Lab run tables, Workflows editor, Attention list, and transcript activity lines.

The prior pinned Hugging Face model-card recipe work is merged in PR #152 and archived at `openspec/changes/archive/2026-09-24-model-card-response-recipes/`. Retain the installed LOW-MTP bundle `bundle_82b658fd3dd6` and standard bundle `bundle_c8ee61eba37d`; the saved LOW configurations include General thinking, Precise coding, and Non-thinking. Preserve the unrelated active `consolidate-product-contract` change. Shell commands must use RTK.
