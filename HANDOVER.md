# Current handover

Updated 2026-09-24. The Chat event-storage repair is validated and running in the established local backend; [PR #136](https://github.com/Vidcar/thtaib/pull/136) is its delivery pointer. Complete `values` records now retain only the latest accumulated snapshot per interaction. Ordered native message/tool events, measurements, lifecycle records, and the durable cursor remain for live resume. Existing databases gain `full_values_seq` on startup and compact older full snapshots at the next complete update. This pass is scoped to backend snapshot retention.

Checks: backend default **694** and integration **168** passed; focused replay/migration tests **13** passed. The local backend was restarted through its supported stop path with no active runs/imports, and the same managed model was reloaded and confirmed healthy. Startup terminal-log maintenance reduced `application.sqlite` from about 531 MB to 27 MB; row counts in 24 non-interaction tables matched the predeploy database. A private temporary backup remains at `.scratch/chat-live-snapshot-predeploy.sqlite` until Dave finishes testing.

Next: Dave tests long live output and returning to a live chat. If lag persists, measure the live replay lock, event delivery, and renderer separately. Prior accepted Unified Workbench delivery is [PR #135](https://github.com/Vidcar/thtaib/pull/135), with [acceptance evidence](openspec/changes/archive/2026-09-24-polish-workbench-surfaces/tasks.md). Deferred benchmark, workflow canvas, media, goals, and background-server work retain their separate OpenSpec boundaries; `consolidate-product-contract` records only four verified overlapping tasks as complete.
