# Current handover

Updated 2026-09-25. Chat helper visibility and Browser/Windows capability repair is complete on main through merged PR #163 (`codex/chat-helper-visual-tools`). Its change is archived at `openspec/changes/archive/2026-09-25-chat-helper-visual-tools/` and synced into the three affected current specs; OpenSpec validates strict. Backend focused checks passed 24 helper and 58 Browser cases. The final backend default suite passed 834 tests (one skipped), integration passed 193, and shared contracts are fresh. Desktop production build, interaction checks and native 360px/600px menu geometry passed. Final native Chat smoke passed.

The previous Chat model-selection/readiness repair is complete and archived at `openspec/changes/archive/2026-09-25-chat-selection-readiness/`; its default, integration, desktop, contract and native checks passed. The older `startup-catalogue` delta retains its own intent.

Next product contract is `openspec/changes/lab-workbench/`, validated strict and not implemented. Lab is split into the destination, Performance (Single stream and Concurrent serving), Memory, and Challenges. The llama-bench and exclusive-hold requirements are removed from that contract. Case capture stays specified for the older screen. Workflows, image, and speech are deferred. The build slices are in that change's tasks.

Final isolated native Chat smoke against the already running Qwen endpoint passed: the full Browser bundle produced real navigate/snapshot calls; one helper appeared live and replayed after reopening; two simultaneous helper calls had distinct scoped transcripts with no sibling marker leakage. Disposable `.scratch` data was removed. The running everyday Workbench session and loaded model were left untouched; reopening the app will load the merged build. No product work remains for this change. Preserve the unrelated Lab draft and saved model weights.
