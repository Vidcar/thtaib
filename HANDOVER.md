# Current handover

Updated 2026-09-24. Chat finalization and streaming repair is complete and merged in [PR #143](https://github.com/Vidcar/thtaib/pull/143). The established desktop and backend were rebuilt and relaunched from `main`; the saved managed Qwen3.8 27B deployment and model weights are healthy. Everyday product data was preserved. Unrelated active OpenSpec changes remain untouched.

Runs now persist a settled outcome, show “Saving project state,” and capture project snapshots outside the shared harness lock with excluded-directory pruning and atomic publication. Generation measurements use a bounded, independent publisher; essential interaction persistence failures are identified explicitly. Interaction metadata and bounded checkpoint linkage reduce repeated history work. The pinned LangGraph SDK patch restores filtered subscription replay and safe reconnects; completed runs compact token deltas while retaining tool and lifecycle identities. Current capability specs were updated, and the completed change is archived at `openspec/changes/archive/2026-09-24-repair-chat-finalization-streaming/`.

Validation: backend default 724 passed (one Windows symlink-privilege skip), integration 184 passed, desktop build including SDK regression checks, shared-contract freshness, and strict OpenSpec validation. An isolated live Qwen2.5 0.5B project run snapshotted the exact tool-written file; a long Chat disconnect/reconnect and compacted replay matched its saved answer. Synthetic medians: excluded-tree snapshot planning 1246.5 → 5.0 ms, and a 4,000-message interaction page 2.245 → 0.007 ms per read. Everyday-workspace latency was not measured. Isolated live test servers and data roots were removed.

No delivery work remains. For future changes, run shell commands through RTK. Backend checks: `uv run --no-sync python -m tests.run` and `--tier integration --durations 10` from `apps/backend`; desktop: `pnpm run build`; shared contracts: `generate_shared_contracts.py --check`; OpenSpec: `openspec validate --all --strict`.
