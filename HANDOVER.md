# Current handover

Updated 2026-09-25. GPU layer controls were corrected on `codex/fix-gpu-offload-controls`. Pinned llama.cpp b11045 treats `--n-gpu-layers -1` as automatic fitting and literal `all` as explicit full offload. The desktop previously displayed `-1` as All and converted a selected All back to `-1`. The fix labels existing `-1` settings Auto, sends literal `all` when All is selected, preserves it through request serialization, and updates backend option descriptions and the Models contract. Dave explicitly asked for the label/control fix only; **do not change his saved model configuration or running deployment**. He will choose performance settings himself. In the preceding live diagnosis, 98k context with MTP yielded about 19 tokens/s and 32k with MTP about 63 tokens/s on the Qwen3.8 27B HF bundle; the prior 98k configuration was restored.

Checks: desktop build and targeted typecheck/request-boundary check passed; 753 default backend tests passed (one skipped); 188 integration tests passed; OpenSpec validation passed (13 items). The code is complete; see Git history for delivery state. The current live model remains untouched. The desktop build is local; a renderer refresh is needed before the open window shows the new control. Preserve the unrelated `consolidate-product-contract` change, user data, and weights.

Previous card-import work: PR #155 is merged; DavidAU standard/LOW-MTP cards and the revision-pinned Unsloth HF bundle were refreshed/imported. See Git history for details.
