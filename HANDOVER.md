# Current handover

Updated 2026-09-25. Goal: simplify model, agent, project, Chat and access setup while keeping Deep Agents central. Active branch: `codex/simplify-chat-model-setup`; change: `openspec/changes/simplify-chat-model-agent-setup/`. Implementation and established local cutover are complete; PR and change archive remain. Factory managed loaded-model limit is one; Dave's install is saved at two. App/chat open stays cold; explicit model selection or first Send loads. Chat owns model/agent/mode/access/capabilities; projects supply folder/knowledge. Incompatible development chats, projects and model setups were retired without compatibility shims.

Isolated Windows checks using installed weights passed: cap-one Qwen→Gemma→Qwen handoff, cap-two simultaneous generations, cap-one/cap-two named Gemma helper with Qwen parent continuation, and concurrent separate chats. Backend default 807 tests (one skip), integration 192 tests, desktop full build, generated-contract freshness, `openspec validate --all` (14/14), and `git diff --check` passed. Main specs are synced; overlapping older `rewrite-loaded-boundaries` delta was reconciled. The old verified Qwen 27B process was stopped; obsolete app records were cleared. All 29 files in six model bundles were unchanged. The rebuilt desktop is open; live API after app/menu open reports six bundles, six configurations, zero chats/deployments, limit two, router stopped and no models loaded. Native visual check confirmed Chat's model picker and no horizontal overflow. Next: create PR, mark final task complete, archive this change, merge and leave this handover at final state.
